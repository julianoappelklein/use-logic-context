{
  "name": "logic-context",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "concurrently \"esbuild ./src/index.jsx --bundle --minify --sourcemap --watch  --outfile=./dist/index.js\" \"live-server ./dist\"",
    "build": "esbuild ./src/index.jsx --bundle --minify --outfile=./dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@types/react": "^17.0.38",
    "@types/react-dom": "^17.0.11",
    "esbuild": "0.14.36",
    "react": "^18.0.0",
    "react-dom": "18.0.0",
    "live-server": "1.2.1",
    "concurrently": "7.1.0"
  }
}
